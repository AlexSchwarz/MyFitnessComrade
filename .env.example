# =====================================================
# Server-Side Environment Variables (Vercel Serverless Functions)
# =====================================================
# These are used by the Vercel serverless functions for USDA API proxy
# Configure these in Vercel Dashboard → Settings → Environment Variables

# USDA FoodData Central API Key
# Get your key at: https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY=your-usda-api-key

# Firebase Admin SDK Service Account (JSON string)
# 1. Go to Firebase Console → Project Settings → Service Accounts
# 2. Click "Generate new private key"
# 3. Copy the entire JSON content and paste it here as a single line
# Note: In Vercel, paste the raw JSON. The serverless function will parse it.
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}
